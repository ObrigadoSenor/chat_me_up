import { library } from '@fortawesome/fontawesome-svg-core';
import { faDeleteLeft, faPaperPlane, faUsersGear, faMessage } from '@fortawesome/free-solid-svg-icons';

export default library.add(faDeleteLeft, faPaperPlane, faUsersGear, faMessage);
