schema: "./server.gql" # GraphQL types (input file)
generates:
  ../client/__generated_types__/types.ts: # Typescript types (output generated file)
    plugins:
      - "typescript"
    config:
      skipTypename: true
